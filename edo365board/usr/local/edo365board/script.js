// Using edo365board 

// Configure
var Config = {
    // Seitenwechsel in Sekunden
    sleep: 5,
    // Update vom Server in Sekunden
    update: 30,
    // Logo Bild in styles (.jpeg / .png)
    // logo: logo.png
}

// start module
edo365board.start(Config);